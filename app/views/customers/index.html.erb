<article ng-app="customers" ng-controller="CustomerSearchController">
  

<header>
  <h2>seach customer</h2>  
</header>

<section class="search-form">
  <form>
    <div class="input-group input-group-lg">
      <label for="keywords" class= "sr-only">Keywords</label>
      <input type="text"
             name="keywords"
             class="form-control input-lg"
             placeholder="First name, Last name, or Email Address"
             ng-model="keywords"
             ng-change="search(keywords)">     
         
    </div>
    </form>
    <h1 class="searchedFor" ng-if="searchedFor">
      <small>Searched for: </small>{{searchedFor}}
      
    </h1>
</section>
<section class="search-results">
  <header>
   <h3>Results</h3>
  </header>
  <%= render partial: "pager", locals: {keywords: @keywords, page: @page} %>
  <ol class="list-group">
   
    <li class="list-group-item clearfix"
      ng-repeat="customer in customers">      
      <h3 class="pull-right">
        <small class="text-uppercase">Joined</small> {{customer.created_at | date}}</h3>
      <h2>
        {{customer.first_name}} {{customer.last_name}}
        
        <small>{{customer.username}}</small>
      </h2>
      <h4>{{customer.email}}></h4>
      
    </li>
    
  </ol>  
  <%= render partial: "pager", locals: {keywords: @keywords, page: @page} %>
</section>

</article>